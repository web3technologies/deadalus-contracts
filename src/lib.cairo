mod example_contracts {
    mod flat_factory;
    mod flat;
}
mod vault {
    mod fraction_vault;
}
mod fraction {
    mod fraction_nft;
}
mod oracle {
    mod time_oracle;
}
mod utils {
    mod storage;
}